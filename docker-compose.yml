version: "3.8"
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: rss1102-blog:dev
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    restart: unless-stopped
